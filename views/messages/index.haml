!!! 5
%head
  %title= 'Sacred Circle Dance Emails'
  %link{:rel => :stylesheet, :type => :"text/css", :href => 'reset.css'}
  %link{:rel => :stylesheet, :type => :"text/css", :href => 'admin.css'}
%body
  %h1 Sacred Circle Dance Emails

  - messages.each_with_index do |message, index|
    .admin-message{class: index.odd? ? 'odd' : 'even' }
      .admin-message-heading
        .admin-id= "#{message.id}."
        .admin-subject= formatted_subject(message)
      .admin-details
        .line= "( by #{message.author})"
        - details_as_array(message).each do |line|
          .line= line
      %hr
      - if message.parsed_date
        .admin-date{class: message.parsed_date ? 'bold' : ''}
          Event found:
          = message.parsed_date

  .admin-footer
    created by
    %a{href: 'mailto:Jack%20Desert<jworky@gmail.com>?subject=Sacred%20Circle%20Events%20Page'} Jack Desert

